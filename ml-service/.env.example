# Python ML Service Environment Configuration
# Copy to .env and adjust paths as needed

# Path to the shared SQLite database (Node writes, Python reads)
DB_PATH=../backend/database.sqlite

# Directory where trained models are saved
MODEL_DIR=./saved_models

# Port for the FastAPI service (do not change — Node expects 5050)
PORT=5050

# Logging level (DEBUG | INFO | WARNING | ERROR)
LOG_LEVEL=INFO
